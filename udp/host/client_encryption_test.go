package host

import (
	"testing"
)

func TestEncryptionHandshake(t *testing.T) {
	// c := clientHost{
	// 	host:                      &host{},
	// 	connection:                inner.New(nil),
	// 	lastEncryptionRequestSend: time.Time{},
	// }
	// err := c.sendEncryptionRequest()
	// require.NoError(t, err)
}
